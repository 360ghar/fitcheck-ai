# Tech debt tracker

Last updated: 2026-08-08 (TD-030–TD-093)

| ID | Item | Severity | Domain | Notes |
|----|------|----------|--------|-------|
| TD-001 | Frontend has no e2e test suite | medium | web | 44 Vitest files / 229 test cases; authenticated Playwright/Cypress E2E still missing |
| TD-002 | ~~No frontend CI workflow~~ | medium | infra | **Fixed 2026-07-26** — `.github/workflows/frontend-ci.yml` added (lint + build on push/PR to `frontend/**`) |
| TD-003 | `docker-compose.yml` exists while local Docker is forbidden | low | infra | Clarify deploy-only vs remove/document |
| TD-004 | Curated `api-spec.md` can drift from OpenAPI | medium | docs | Prefer live OpenAPI; regen or spot-check |
| TD-005 | `docs/generated/db-schema.md` can drift from migrations | medium | docs | Run generate script when migrations change |
| TD-006 | Duplicate/overlapping product notes (specs + old feature notes) | low | docs | Consolidate over time |
| TD-007 | Flutter architecture doc still thin | medium | mobile | Expand `FLUTTER.md` as decisions accrue |
| TD-008 | DESIGN.md is draft | low | design | Flesh out tokens/patterns when brand solidifies |
| TD-009 | Job workers are not restart-resumable | high | reliability | **Partially fixed 2026-07-31** — batch extraction and photoshoot metadata/final storage URLs persist in hosted Supabase and status/SSE recovery validates ownership; an active provider pipeline is polling-only after process restart, and social-import workers remain process-local. |
| TD-010 | Optional Pinecone paths under-documented operationally | low | backend | Config-sensitive recommendations |
| TD-011 | ~~Frontend CI (lint/build) not wired~~ | medium | infra | **Fixed 2026-07-26** — same workflow as TD-002 (`frontend-ci.yml`) |
| TD-012 | Flutter package CLAUDE.md missing | low | mobile | Root map covers mobile via docs/FLUTTER.md |
| TD-013 | Route module LOC taste lint not mechanical | low | backend | Deferred from harness plan §6: soft “routes stay thin” guidance only; no automated LOC ceiling in `check_architecture.py` |
| TD-014 | ~~Dead SQLAlchemy imports of missing `app.db.base_class`~~ | medium | backend | **Fixed 2026-07-26** — worse than recorded: the modules were un-importable, not merely carrying dead imports (`sqlalchemy` is absent from `requirements.txt`, `app.db.base_class` never existed, `app/models/` has no `__init__.py`, zero referrers). Both files deleted. Original note: `models/calendar.py` and `models/gamification.py` import `from app.db.base_class import Base` but the module does not exist (Supabase-first stack). Clean up or remove legacy ORM stubs |
| TD-015 | ~~Flutter route-refresh workers are dead code~~ | medium | mobile | `ever(Get.routing.obs, …)` in `wardrobe_controller._setupRouteListener` and `debounce(Get.routing.obs, …)` in `outfit_list_controller._setupRouteListener` each allocate a **fresh** `Rx<Routing>` around a snapshot that is never written, so neither handler can ever fire. "Refresh wardrobe/outfits after returning from add/edit" has therefore never worked; the compensating cross-controller calls in `item_add_controller` (749/850), `batch_extraction_controller` (511/1367), `outfit_creation_controller:136` and `outfit_generation_controller:121` exist to paper over it. Replace with a `NavigatorObserver` (`AppRouteObserver` already exists) or an explicit refresh signal, then reassess whether the cross-controller writes are still needed. **Fixed 2026-07-26** — deleted rather than resurrected. Every save path already notifies explicitly (`WardrobeController.addItems` from item add, `fetchItems(refresh: true)` from item edit and batch extraction, `fetchOutfits(refresh: true)` from outfit generation), so making the workers live would only have added redundant refetches. If route-driven refresh is ever needed, `AppRouteObserver` is already registered in `GetMaterialApp.navigatorObservers` and is the correct mechanism — `Get.routing.obs` never was. Original note: Found 2026-07-26 while fixing the Obx markNeedsBuild-during-build class |
| TD-016 | ~~`_read_upload_capped` duplicated between `batch_processing.py` and `app/core/uploads.py`~~ | low | backend | **Fixed 2026-07-26** — private copy deleted, `batch_processing.py` now imports the shared helper. Original note: Phase 2E moved the canonical chunked-read helper to `app/core/uploads.py::read_upload_capped` (fixing the hardcoded 7MB in its `FileTooLargeError`) for `items.py`/`users.py`/`outfits.py`/`feedback.py`. `batch_processing.py::_read_upload_capped` (~:68) was left in place because that file was owned by a parallel agent. Delete the private copy and import the shared one. Found 2026-07-26 |

| TD-017 | ~~~48 blocking sync `.execute()` calls remain under `app/services/`~~ | medium | backend | The route layer is done: all 213 bare `.execute()` calls inside an `async def` in `app/api/v1/` now go through `asyncio.to_thread`, and `tests/test_small_routes_async.py` guards it. `app/services/` still has ~48 un-wrapped calls (21 already wrapped). Same fix, same form; **Fixed 2026-07-26** — all 69 converted with the same `await asyncio.to_thread(<chain>.execute)` form, and `tests/test_small_routes_async.py` now scans `app/services/` as well as `app/api/v1/`. 310 offloaded call sites across `app/`. Found 2026-07-26 |
| TD-018 | Photoshoot cancellation is advisory and still bills | medium | backend | `photoshoot.py` cancel flips a flag and broadcasts, but in-flight image generations run to completion and are still charged. The fix is a cancel check inside the generation loop between provider calls, not at the route. This is a money bug, not only a UX one. Found 2026-07-26 |
| TD-019 | ~~~`POST /photoshoot/generate?sync=true` holds an HTTP request open for minutes~~~ | medium | backend | Contradicts `docs/RELIABILITY.md` ("job stores + SSE rather than single multi-minute HTTP requests"). Unlike the async path it creates no job record, so a proxy timeout or client drop loses the generation unrecoverably, and it does not pre-check the daily limit at the route. **Fixed 2026-08-03 (photoshoot speed pass)** — the web app migrated to the async job + SSE flow (`startPhotoshootJob` → `subscribeToPhotoshootEvents` with poll fallback); `retryFailedSlot` uses the same flow. The `sync=true` endpoint remains as a legacy compatibility path for any remaining callers. Found 2026-07-26 |
| TD-020 | SSE terminal event names differ between streams | low | backend | Batch and photoshoot emit `job_complete`; social import emits `job_completed`. Same drift class this pass exists to remove, but renaming breaks live clients, so it needs a deprecation window. Found 2026-07-26 |
| TD-021 | Four incompatible job-status enums, no shared base | medium | backend | `BatchJobStatus` (`batch_job_service.py`), `PhotoshootJobStatus` + `PhotoshootStatus` (`models/photoshoot.py`), `SocialImportJobStatus` (`models/social_import.py`), `GenerationStatus` (`models/outfit.py`) all model the same "job status" concept with different vocabularies (`"completed"` vs `"complete"`, `PROCESSING` vs `EXTRACTING`/`GENERATING`, extra states like `AWAITING_AUTH`/`PAUSED_RATE_LIMITED`). Each also duplicates its own terminal-status guard logic (see TD fix that added the guard to `PhotoshootJobService.update_status`, mirroring `BatchJobService`). A shared base (terminal-set mixin or common enum values) would remove this drift, but touches four call sites; not attempted here. Found 2026-07-26 |
| TD-022 | ~~`extraction_jobs` DB table defined but never referenced in code~~ | low | backend | **Fixed 2026-07-31** — `BatchJobService` persists metadata and final storage URLs through migration `023_durable_job_state.sql`; active pipelines remain polling-only after a process restart (TD-009). |
| TD-023 | `POST /api/v1/items/upload` returns 202 Accepted but runs synchronously | low | backend | `backend/app/api/v1/items.py:86` sets `status_code=status.HTTP_202_ACCEPTED`, but the handler uploads all files via `parallel_with_retry` and returns the final result in the same response — there is no job to poll. 202 implies async acceptance; this should be 200/201. Misleading but not a live bug. Found 2026-07-26 |
| TD-024 | `outfit_generations.status` never reaches `FAILED` | low | backend | `GenerationStatus.FAILED` (`models/outfit.py`) is never set anywhere in `backend/app` (grep confirms). However both client polling paths that would read it are already dead code: web's `outfitStore.checkGenerationStatus` (`frontend/src/stores/outfitStore.ts:755`) is defined but never called from any component, and Flutter's `OutfitGenerationController._pollGenerationStatus` is only reached via `generateVisualization`, which likewise has no caller in `flutter/lib`. Non-user-facing debt, not a live bug; do not fix the FAILED-setting gap until a live caller exists. Found 2026-07-26 |
| TD-025 | `OutfitRepository.getGenerationStatus` (Flutter) now orphaned | low | mobile | Its only caller, `OutfitGenerationController._pollGenerationStatus`, was deleted as dead code in the same pass that created TD-024 (that controller's `generateVisualization`/`_pollGenerationStatus` had zero callers anywhere in `flutter/lib`). `grep -rn "getGenerationStatus" flutter/lib flutter/test` now shows zero callers of the repository method. Left in place rather than deleted here — it returns the freezed `GenerationStatus` model (`outfit_model.dart`), and removing it would touch generated `.freezed.dart`/`.g.dart` code, a bigger blast radius than this pass's cleanup scope. Delete both the method and the model when someone next touches this repository. Found 2026-07-26 |
| TD-026 | Gemini provider has no cross-provider fallback | medium | backend | `GeminiProvider.chat_with_vision` only falls from a Gemini model to a configured Gemini fallback model (`AI_GEMINI_VISION_FALLBACK_MODEL`) — unlike the Custom/Agnes provider, it cannot fall through to a different vendor entirely if Google's API has an account-wide outage. Acceptable today because Gemini is opt-in (`AI_DEFAULT_PROVIDER` defaults to `custom`); revisit if Gemini becomes the default. Found 2026-07-27 |
| TD-027 | Gemini provider has no `response_schema` structured-output parity | low | backend | `response_format={"type":"json_schema",...}` (the `MULTI_ITEM_RESPONSE_FORMAT` extraction contract) maps only to `response_mime_type="application/json"` for Gemini, not a translated `response_schema` — OpenAI's schema dialect (`["string","null"]` unions, `additionalProperties: false`) isn't valid in Gemini's OpenAPI-3.0 subset. No current caller passes a schema Gemini could use anyway. Add a schema translator only if extraction quality on Gemini needs it. Found 2026-07-27 |
| TD-028 | ~~Gemini provider has no health-check circuit breaker~~ | low | backend | **Superseded 2026-07-31** — PR #9 (`a124226`) closed the operational need with a stateless design instead of a stateful breaker: `classify_gemini_error()` (daily free-tier quota → not retryable → immediate Agnes fallback; per-minute → retryable after parsed `RetryInfo.retryDelay`), `retry_after_seconds` honored by `with_retry`, batch/social `capacity_exhausted` flags that stop grinding guaranteed-fail calls, and `config_health` warning when the Agnes fallback key is missing. A general non-quota Gemini outage still pays a request timeout (deliberate, as documented in `docs/exec-plans/completed/2026-07-27-native-gemini-provider.md`). See `docs/exec-plans/active/quota-fallback-and-upgrade-propagation.md`. Found 2026-07-27 |
| TD-029 | Hybrid vision-leg native-Gemini routing is system-config only, no BYOK support | low | backend | `AI_VISION_PROVIDER=gemini` (`ProviderConfig.vision_provider`/`vision_gemini_api_key`, `AIProviderService._chat_with_vision_via_native_gemini`) is populated only by `ProviderConfig.from_settings()` — `from_user_dict()` (BYOK) never sets these fields, so a user configuring the Custom provider through AI settings always gets plain OpenAI-compatible vision, never the hybrid Gemini-primary path. Deliberate v1 scope decision, not a bug. Extend `from_user_dict()` (and the `ProviderConfigInput` schema in `models/ai.py`) if per-user hybrid routing is ever needed. Found 2026-07-27 |

| TD-030 | Referral credit always grants Pro, never Plus | low | backend | `SubscriptionService.apply_referral_credit` hardcodes `plan_type="pro_monthly"` when upgrading a free user to a referral trial. With the Plus tier live (see `docs/exec-plans/active/2026-07-31-plus-plan.md`) a referral now hands out the most expensive tier for free. Decide whether a referral should grant Plus instead, and whether `referral_credit_months` should be denominated in a tier at all. Found 2026-07-31 |
| TD-031 | No in-app expiry for any paid plan | medium | backend | `is_paid_plan()` checks only `plan_type`, so a subscription whose `current_period_end`/`trial_end` has passed stays entitled until the external `backend/scripts/revert_expired_pro_trials.py` cron downgrades it. Pre-existing for Pro; now applies to Plus too. The fix is a period-end check in the entitlement predicate (or a scheduled job owned by the app, not an ops script). Found 2026-07-31 |
| TD-032 | `SubscriptionPanel` fetches `/plans` but renders local constants | low | frontend | `frontend/src/components/settings/SubscriptionPanel.tsx` calls `fetchPlans()` and uses the response only as a render gate (`!isPro && plans?.plans`); the displayed prices/limits come from `frontend/src/lib/plan-limits.ts`. Backend and web therefore hold two copies of every price. Either render from the fetched payload or drop the fetch. Found 2026-07-31 |
| TD-033 | Provider image-count limit for outfit garment references is unverified | medium | backend | Outfit generation now sends the avatar plus one reference image per item, deliberately uncapped (see `docs/exec-plans/active/2026-07-31-outfit-item-reference-images.md`). `ai_provider_service._generate_image_via_images_api` puts them all in `extra_body.image[]`, but whether `agnes-image-2.1-flash` honours more than the first few is unknown — if it truncates, the prompt still promises an `IMAGE n` the model never received, which invites hallucination. The Gemini leg (`_messages_to_contents`, one `Part` per image) is structurally sound. Probe with a 3-item outfit of unmistakable garments and check whether the LAST one is reproduced; log line `AI image generation request started` carries `reference_images=`. If it truncates, route this endpoint to the Gemini image leg or `AI_IMAGE_API_STYLE=chat`. Found 2026-07-31 |

| TD-034 | Flutter dashboard's optional streak request must not block the home screen | resolved | mobile | `flutter/lib/features/dashboard/controllers/dashboard_controller.dart` now isolates the optional streak failure from dashboard loading, with a regression test in `flutter/test/features/dashboard/controllers/dashboard_controller_test.dart`. Keep the router mounted while the feature remains flag-gated. Resolved 2026-07-31 |

| TD-035 | Off-palette status hues remain in the settings and social-import panels | low | frontend | ~56 `bg-{orange,blue,amber,green,red,sky,emerald,rose,violet}-NNN` literals survive across `components/settings/*`, `components/wardrobe/SocialImport*`, `BatchImageSelector`, `ExtractedItemsGrid` and `BatchExtractionFlow`. **These are not a dark-mode defect** — every one has a `dark:` pair and `scripts/check_theme_tokens.py` passes, so they render correctly in both themes. They are a *hue-consistency* break: DESIGN.md 01 specifies one accent, and a page peppered with five tinted status families is the "labels as tinted pill chips" tell. The primary surfaces were converted in the 2026-07-31 pass to one vocabulary — an opaque surface chip (`bg-background/90 border-border`) carrying a small desaturated dot, with `--event-*` and `--condition-*` families in `index.css` as the precedent to copy (`ItemCard`, `ExtractedItemCard`, `CalendarView`, `Batch*Progress`). Do the remaining panels the same way; do not invent a sixth palette. Found 2026-07-31 |
| TD-036 | ~~Public Supabase storage buckets and raw URLs remain~~ | high | security | **Fixed 2026-08-04** — the Railway bucket migration moved storage off Supabase Storage into a private S3-compatible bucket; read paths materialize short-lived presigned URLs (or worker-mode URLs, 2026-08-05) and ownership validators gate upload/read. Original note: `StorageService` calls `get_public_url` for item uploads and the initial migration creates a public bucket. Ownership checks do not revoke a raw URL. Private buckets/signed URLs are deferred in the active hardening plan. |
| TD-037 | Cross-client verification gap | high | quality | No authenticated browser E2E or Flutter `integration_test/`; backend route checks commonly direct-call handlers and bypass full auth wiring. The ledger marks these stories implemented-unverified rather than passed. |
| TD-038 | Flutter local SDK cache permission blocker | medium | mobile/infra | The default sandbox may not write the Flutter SDK cache. Verification passed with an approved writable SDK cache; keep CI as the reproducible release boundary and do not alter the SDK from the repository. |
| TD-039 | Stale aggregate/status documentation | medium | docs | The former 77% implementation summary and old test counts overstated completion. The story ledger is now authoritative; curated API docs remain non-canonical. |
| TD-040 | ~~Active/completed plan lifecycle drift~~ | low | process | **Resolved 2026-08-08** — `cross-platform-wardrobe-studio.md` now lives at `docs/exec-plans/completed/2026-07-31-cross-platform-wardrobe-studio.md`. The 2026-08-08 plan GC also found four plans **duplicated** (not moved) across `exec-plans/active/` and `exec-plans/completed/`: free-users trial campaign, railway-bucket migration contract, app-store readiness review, and auto-outfit-from-upload. The completed copies are canonical and the four duplicate `active/` copies were deleted. Original note: `cross-platform-wardrobe-studio.md` is marked completed while remaining under `exec-plans/active/`; reconcile during plan GC, without treating plan prose as verification evidence. |
| TD-041 | No lazy server-side re-verification of stored store transactions | medium | backend | After mobile IAP ships, `GET /subscription` trusts the stored row until a webhook or the expiry cron touches it. A missed App Store/Play webhook leaves a stale entitlement until the next renewal/expiry event (or the `revert_expired_pro_trials.py` cron, TD-031). Defense in depth: lazily re-verify the stored `apple_original_transaction_id` / `google_purchase_token` with the store APIs on `GET /subscription` (rate-limited, cache per row) — deferred from the mobile-IAP plan (`docs/exec-plans/completed/2026-08-01-mobile-iap-subscriptions.md`). Found 2026-08-01 |
| TD-043 | Supabase singleton connection recovery is per-call-site | medium | backend | `is_db_connection_error` + `execute_with_reconnect` (app/utils/db.py, added 2026-08-01) harden the hot paths seen failing in the 2026-08-01 logs. 2026-08-03 extended coverage to the paths that still 500'd: `get_subscription` (heals `/subscription`, `/referral/*`, `/users/dashboard`), `list_outfits`, referral service, `/users/me` + `/settings` + `/preferences`, `reserve/release_ai_usage`, Supabase Storage uploads, plus `deque mutated during iteration` marker. A second 2026-08-03 pass closed the rest: `get_or_create_usage_record` (the remaining unwrapped call behind "Error getting usage" 500s; insert is now an idempotent `on_conflict` upsert), referral `validate`/`redeem`, promo `validate`/`redeem` (RPCs are single transactions, so reconnect retries cannot double-grant), photoshoot `get_or_create_daily_usage`, and detection was extended to numeric-only error text (bare h2 `error_code`: `11`/`41`/`45`/`79`/`81`), `"invalid input"` (`ProtocolError` state strings), and `"server disconnected"`. Full fix remains the async-client migration tracked in `app/db/connection.py`; wrap remaining call sites as they surface. |
| TD-044 | No app-wide cap on concurrent image generation | medium | backend | 2026-08-03: container OOM ("Killed") during a try-on/image-gen storm - each request buffers multi-MB base64 images. `PHOTOSHOOT_CONCURRENCY_LIMIT=2` bounds photoshoot only; try-on/outfit generations were unbounded. **Fixed 2026-08-03 (second pass)** — `app/core/concurrency.py` gained `image_gen_slot()`, a reentrant per-task wrapper over the process-wide `GENERATION_SEMAPHORE` (AI_GENERATION_CONCURRENCY, default 30); wired into `_generate_image`, `_generate_with_references`, `generate_try_on`, both photoshoot sites (local fan-out cap retained alongside), and batch/variations converted from the raw semaphore to the slot. Reentrancy via ContextVar so nested entry points (variations → generate_outfit) can't deadlock. |
| TD-042 | Flutter batch flow drops `source_image_url` when saving items | low | mobile | The web batch flow now surfaces the SSE payload's `source_image_url`/`source_image_storage_path` and persists both in `createItem` so the auto-outfit upload flow can use the original photo as an "as worn" generation reference (`docs/exec-plans/active/2026-08-01-outfit-source-photo-reference.md`). Flutter's `DetectedItemData` freezed model + create payload still drop the fields; only matters once Flutter auto-outfit lands (freezed `.g`/`.freezed.dart` regeneration deferred). Found 2026-08-01 |
| TD-045 | `reserve_ai_usage` still not reconnect-retried (fail-closed by design) | low | backend | Review follow-up 2026-08-03: the `execute_with_reconnect` wrap was removed from `reserve_usage` because the RPC is a non-idempotent counter increment — an automatic retry after a lost response would double-reserve daily quota or return false against the inflated counter while the first reservation stays consumed. `release_usage` keeps the wrap (GREATEST(0, …)-bounded). If the 2026-08-03 "Failed to reserve AI usage" dead-connection bursts recur, make the RPC idempotent (client-supplied reservation key + unique constraint, `ON CONFLICT DO NOTHING`) via a migration, then re-wrap. Found 2026-08-03 |
| TD-046 | Flutter manual-entry dead-end fixed; `showManualEntry` + `selectedImage` interplay remains fragile | low | mobile | Review follow-up 2026-08-03: `item_add_page.dart` now renders `ManualEntryForm` whenever `showManualEntry` is true (before the AI-extraction branch), so "Enter Manually" works with a photo selected. The controller's `proceedToManualEntry` still only flips the flag; if the page gains new branches, keep the manual-entry branch first. Found 2026-08-03 |
| TD-047 | `AIServiceError` carries parallel `retryable` + `fallback_eligible` booleans | low | backend | `app/core/exceptions.py` — every call site unions them (`retryable or fallback_eligible`). A single tri-state (`retry_policy: none | same_model | fallback_model`) would encode the decision once; deferred because it touches every raise/catch site. Found 2026-08-03 (simplify pass) |
| TD-048 | Error-text heuristics for provider/DB failures are brittle string matching | low | backend | `_is_content_policy_rejection` (ai_provider_service) substring-matches provider copy ("unable to generate this content"); `is_db_connection_error` (utils/db.py) treats any numeric-only error text as a connection error. Both silently change behavior when provider/library wording changes. Deliberate for now — the markers are documented and log-centralized; centralize into constants if they drift again. Found 2026-08-03 (simplify pass) |
| TD-049 | `outfitStore` mirrors generation state in two places | low | frontend | `generatingOutfits` map (grid cards) is hand-synced with `isGenerating`/`generationStatus` scalars (detail pane) in the regenerate path — the drift between them was the exact bug the sync fixes, and every future status change must remember both. Deriving the scalars from the map is deferred: two documented UI surfaces, tested, and the refactor risk exceeds the benefit today. Found 2026-08-03 (simplify pass) |
| TD-050 | `users.py::_get_or_create_record` costs 3 round trips on the create path | low | backend | SELECT → idempotent upsert → re-SELECT. The re-read is a deliberate concurrency-correctness choice (the upsert response is authoritative); optimize by returning the upsert's own row and re-reading only on conflict/None. Found 2026-08-03 (simplify pass) |
| TD-051 | Batch generation holds the global image-gen slot during storage uploads | low | backend | Each generated image's multi-MB base64 decode + upload runs inside `async with image_gen_slot()` (batch_extraction_service `_generate_single_item`), so pure I/O occupies a process-wide generation slot. Release the slot before the upload with a small dedicated upload semaphore. Deferred: delicate memory-hardened code, low severity. GC thresholds (`gc.set_threshold(700, 5, 5)`, main.py) also untuned — gen2 stop-the-world collections can add jitter during AI bursts; needs measurement before adjusting. Found 2026-08-03 (simplify pass) |
| TD-052 | Web batch store pins upload files + previews for the whole SPA session | low | frontend | `batchExtractionStore` keeps up to 50 `File` refs plus base64 previews until an explicit reset (cancel / save-complete / close) — the old `useState` freed them on unmount. Drop `file` refs once upload completes (previews suffice for review) or clear on terminal events. Also: `wardrobeStore.fetchMore` re-sorts the whole accumulated list per page (O(n² log n) across a session) and ignores `search` server-side — incremental insert into the sorted array, and send `search` to the server. Found 2026-08-03 (simplify pass) |
| TD-053 | Misc simplify-pass skips | low | cross | `SubscriptionPanel` "Card payments are being set up" block duplicated across two cards (extract a small component); `outfits.py::_build_list_query` takes two mutually exclusive boolean flags (enum or separate builders); `tryon_page.dart` duplicates the glass-card Stack wrapper and re-runs `Get.find<TryOnController>()` inside `_buildReportBadge`; backend tests duplicate store/job fixtures per file instead of `conftest.py`; `upload_temp_generated_image` full-decodes every generated image to validate — sniff magic bytes first (`sniff_image_mime_from_magic`), decode only on sniff failure. Found 2026-08-03 (simplify pass) |
| TD-054 | ~~Gemini daily-quota cap still burned a request per call before falling back~~ | medium | backend | Observed 2026-08-03 evening: the free-tier 20/day cap exhausted, and every subsequent extraction still paid one Gemini call + `[err]` log line before the Agnes fallback (dozens across 17:49–20:24). **Fixed 2026-08-04** — `gemini_provider.py` gains a process-local daily-quota latch keyed by API-key hash (tripped on `perday` classification, reset at midnight US/Pacific): `chat()` fails fast with `upstream_quota` before building a request, so the hybrid vision leg falls to Agnes with zero wasted calls. Quota failures are logged at WARN (they are expected once the cap is hit, not errors). Tests in `test_gemini_provider.py`. Found 2026-08-03 |
| TD-055 | ~~`get_user_settings` default-row insert raced into 23505~~ | medium | backend | `get_user_settings` created the `user_ai_settings` row with a plain `insert` while `ensure_ai_settings_row` used an upsert — concurrent first admissions (select-miss race) hit `user_ai_settings_pkey` 23505 → `GET /ai/settings` 503 (observed 2026-08-03 20:02). **Fixed 2026-08-04** — creation is now `upsert(on_conflict="user_id")` + re-select, identical to `ensure_ai_settings_row`. Tests in `test_ai_settings_service.py`. Found 2026-08-03 |
| TD-056 | ~~Register password policy mismatch web/backend~~ | medium | backend | Web form deliberately gates only on 8+ chars ("strength checklist is guidance, not a hard gate"); mobile signs up via Supabase with no strength rule; backend `/auth/register` hard-required upper+lower+digit+special → every such signup 422'd (~40 across 18:01–20:00, observed 2026-08-03). **Fixed 2026-08-04** — `RegisterRequest` is length-gated only; `ConfirmResetRequest` (password reset) keeps the full-strength rule since the reset page gates client-side. Tests in `test_auth.py`. Found 2026-08-03 |
| TD-057 | ~~JWKS re-fetch churn on expired access tokens~~ | low | backend | `_decode_asymmetric` treated `ExpiredSignatureError` like a stale-cache failure: every app resume fired a JWKS re-fetch + two warn lines per request (observed 2026-08-03 18:06/18:39/19:23/19:53 bursts). **Fixed 2026-08-04** — expiry propagates without a JWKS reset (re-fetch cannot help: expiry is checked against `exp`, not the signing key) and is logged at DEBUG; the re-fetch stays for unknown-kid/rotation. Tests in `test_auth.py`. Found 2026-08-03 |
| TD-058 | ~~Upload retry loop burned 4 attempts on permanent 4xx~~ | low | backend | `parallel_with_retry` retried every exception type, so "Uploaded bytes are not a valid image" (`UnsupportedMediaTypeError`, a permanent 415) ran 3 extra decode/upload cycles per file ("All 4 attempts failed", observed 2026-08-03 18:10). **Fixed 2026-08-04** — `parallel_with_retry` accepts a `should_retry` predicate; the items upload excludes `UnsupportedMediaTypeError` (transient storage errors still retry). Tests in `test_parallel_retry.py`. Found 2026-08-03 |
| TD-059 | ~~Server-busy 429 mislabeled as the user's plan limit~~ | medium | backend | The concurrent-batch-job cap raised `RATE_LIMIT_EXCEEDED`, so the web client treated server capacity as the user's own quota: no retry + upgrade prompt (observed 2026-08-03 18:08/18:26: "Server is busy processing 2 batch jobs" as a 429). **Fixed 2026-08-04** — the cap raises the new `SERVER_BUSY` code (429, retry_after=60, user-facing message); web retries it with backoff and toasts "Server Busy"; Flutter's Dio mapper now preserves the backend code/message on 429 and routes `SERVER_BUSY` to the capacity dialog, never the paywall CTA. Tests in `test_batch_job_memory_lifecycle.py`. Found 2026-08-03 |
| TD-060 | ~~Stripe checkout/portal gate drifted from /plans~~ | low | backend | `/checkout` and `/portal` only checked `STRIPE_SECRET_KEY` while `/plans` required the key + all four price IDs, and the 503 said "contact support" for what is an env gap. **Fixed 2026-08-04** — shared `_stripe_billing_configured()` gates all three; the 503 message points at promo codes as the working path. The real fix remains ops: set the five Stripe vars (runbook in the 08-03 RCA plan, still unapplied). Found 2026-08-03 |
| TD-061 | ~~`/promo/validate` 422 on partial codes~~ | low | backend | `ValidatePromoRequest.code` had `min_length=3` while the landing/register pages validate as the user types; the service already normalizes and returns a friendly `valid=False`. **Fixed 2026-08-04** — `min_length=1`. Test in `test_promo_api.py`. Found 2026-08-03 |
| TD-062 | Banked `referral_credit_months` are never consumed after a paid period ends | medium | backend | Migration 033's `apply_referral_credit_atomic` banks months (`referral_credit_months += p_months`) for paying subscribers and never overwrites them, but nothing ever converts the bank into trial time when the paid period later ends — a paying user who referred friends gets the months recorded but no entitlement after their subscription lapses. Decide a consumption rule (e.g. on downgrade-to-free, grant `trial_end = now + referral_credit_months` and zero the bank) or document banking as purely informational. Found 2026-08-04 (referral-redemption RCA, docs/exec-plans/active/2026-08-04-referral-redemption-rca.md) |
| TD-063 | ~~Storage migration: old-style keys still in bucket until folder hardening; presigned URLs are short-lived~~ | medium | backend | **Resolved 2026-08-05** — the Railway Bucket migration landed (contract: docs/exec-plans/completed/2026-08-04-railway-bucket-migration-contract.md): new key layout `{user_id}/{category}/{uuid}.{ext}` is the only live layout, `migrate_legacy_keys_to_new_layout.py` rewrote legacy keys + DB columns, and `storage_inventory.py` cleans orphans. The short-lived-presigned-URL half is superseded by the egress RCA (TD-064/TD-065): worker-mode stable URLs + real thumbnails + client caching. Original note: legacy Supabase keys (`{bucket}/{uuid}.{ext}`) remain in the bucket until `scripts/migrate_storage_to_railway.py` (planned) + `backend/scripts/storage_inventory.py` clean them up. Image URLs are short-lived presigned GET URLs (~15 min), so clients must re-fetch as they expire; `STORAGE_BACKEND=supabase` remains as a cutover fallback flag. Found 2026-08-04 |
| TD-064 | Railway egress: R2 cutover is code-complete but not applied to prod | medium | infra | Egress RCA (`docs/exec-plans/active/2026-08-05-railway-egress-rca.md`): `migrate_storage_to_r2.py` + provider-agnostic config are landed and tested, but the ops runbook (create R2 bucket/keys → `--apply` copy → repoint `OBJECT_STORAGE_*` → empty the Railway bucket) has not run, so the $0-egress win is not yet live. Apply per the exec plan's Ops checklist. Added by the 2026-08-05 review: the R2 bucket also needs a CORS policy for the web origins (the frontend fetch()es image URLs), migration `036_widen_image_url_columns.sql` must be applied, and cleaning the OLD bucket now requires the explicit `--endpoint/--bucket` overrides (a bare `--delete` inspects whatever is configured, i.e. R2 after cutover). Found 2026-08-05 |
| TD-065 | Worker-mode serving + thumbnails are flag-gated; rollout pending deploy | medium | infra | `IMAGE_SERVING_MODE=worker` (stable cacheable URLs via `infra/images-worker`), `THUMBNAIL_SERVING=true` (`_thumb` siblings) and the Flutter/web cache changes are landed and tested, but defaults remain `presigned` + `false` until the Worker is deployed and thumbnails are backfilled (`generate_thumbnails.py --apply`). Rotating presigned URLs were the dominant egress driver; each of these flips is independent. Added by the 2026-08-05 review: the Worker needs `ALLOWED_ORIGINS` set, and `THUMBNAIL_SERVING=true` must wait for a clean `DRY_RUN=0 generate_thumbnails.py` run (a non-zero exit means some objects have no thumb and would 404). Found 2026-08-05 |
| TD-066 | Backend→R2 AI reference fetches remain Railway service egress | low | backend | With storage on R2, `GeminiProvider` / `item_reference_service` server-side downloads of presigned URLs leave Railway (service→Cloudflare egress) for every AI call that includes image references. Bounded (10 MB, downscaled references) and only on AI-write paths; if the bill ever shifts to the AI path, cache reference bytes server-side (TTL keyed by object key) or move the fetch into the Worker. Found 2026-08-05 |
| TD-067 | Worker edge cache serves any caller who knows the path until TTL | low | security | Accepted trade-off in worker mode (documented in `docs/SECURITY.md` + `infra/images-worker/README.md`): edge-cached objects are served without per-request auth. Paths are unguessable (`{user_id}/{uuid}`), URLs are only issued by ownership-checked responses, and objects are write-once per key. If hard per-request auth is ever required, move to signed cookies with a path-only cache key. Found 2026-08-05 |
| TD-068 | Worker-mode web images 404: frontend never sets the auth cookie | medium | frontend | The Worker accepts `Authorization: Bearer` (Flutter) or the `sb-<ref>-auth-token` cookie (web), but `frontend/src/lib/supabase.ts` uses the default browser `createClient` (localStorage persistence) which sets NO cookies, and `<img>` cannot set headers — so in `IMAGE_SERVING_MODE=worker` every web image would 404. Fix (runbook in `infra/images-worker/README.md`): set a `Domain=<registrable domain>; Path=/; Secure; SameSite=Lax` cookie from the session on login/refresh (`onAuthStateChange`), so same-site subresource requests to `images.fitcheckaiapp.com` carry it. Until then keep the web on presigned mode (R2 egress is $0 — the web loses cost-wise nothing, only cache hits). Added by the 2026-08-05 review: the cookie alone is insufficient for the four `fetch()` download/share paths, which default to `credentials: 'same-origin'` and so send no cookie cross-origin — they need `credentials: 'include'` (the Worker already echoes an exact origin plus `Access-Control-Allow-Credentials`), and `crop-from-bounding-box.ts`'s `crossOrigin='anonymous'` sends no credentials at all. Full matrix in `infra/images-worker/README.md`. Found 2026-08-05 (self-review of the egress RCA) |
| TD-069 | Shared image executor is the upload-duration bottleneck | medium | backend | Every Pillow decode/validate + thumbnail + S3 PUT runs through one process-wide `ThreadPoolExecutor(max_workers=IMAGE_PROCESS_WORKERS, default 4)` (`app/core/image_executor.py`). Under concurrent batch-extract + uploads, requests queue for tens of seconds, which surfaced as the late multipart 400s on `/items/upload`, `/users/me/avatar`, `/outfits/{id}/images` (2026-08-05). The 2026-08-05 RCA extended the *frontend* timeout for those paths (`LONG_RUNNING_PREFIXES`) as a workaround; the real fix is to raise `IMAGE_PROCESS_WORKERS` (and/or move S3 PUTs off the bounded pool) and measure p99 upload latency. Found 2026-08-05 |

| TD-070 | Saved AI renders (`{user}/generated/…`) are not DB-referenced | medium | backend | A `save_to_storage=true` try-on / outfit render is written to `{user_id}/generated/{image_type}/{uuid}.{ext}` and returned to the client as a URL only — no DB row references it. Consequences, both worked around rather than fixed on 2026-08-05: the orphan inventory flags every one (mitigated by a 30-day `CATEGORY_MIN_AGE_HOURS['generated']` retention window instead of the 2h transient window that was deleting them), and no read path re-materializes the URL (mitigated by adding the category to the `/images/presigned` key allowlist, though no client calls that endpoint yet). Real fix: reference them from a DB row and route them through `materialize_image_urls` like every other category, or make them explicitly ephemeral in the API contract. Found 2026-08-05 (storage-migration review) |
| TD-071 | Expiring presigned URLs are still persisted in DB columns | low | backend | `item_images.image_url` / `thumbnail_url`, `outfit_images.*` and `users.avatar_url` are written at upload time with the LIVE presigned URL even though the durable reference is `storage_path` and every read path re-materializes. The stored value is dead within `OBJECT_STORAGE_PRESIGN_TTL`, so it is a trap for any new read path that forgets to materialize — exactly how the leaderboard bug (fixed 2026-08-05) happened. Migration `036` widened the columns to TEXT so R2's longer URLs cannot overflow them, but the columns should stop holding URLs at all; blocked by `image_url NOT NULL` (carried over from `docs/exec-plans/completed/2026-08-04-railway-bucket-migration-contract.md`, Follow-ups section). Found 2026-08-05 |
| TD-072 | Migration 034 key rewrite never re-ran for ticket attachments | low | backend | The 2026-08-04 legacy-key rewrite skipped `support_tickets.attachment_storage_paths` because migration 034 was not yet applied to the live schema, and the promised re-run has no record (see the progress log in `docs/exec-plans/active/2026-08-04-legacy-key-migration-and-review-fixes.md`). Any ticket attachment predating that migration may still carry an old-layout key. The rewrite script was retired with the 2026-08-07 cleanup, so the surviving verification path is `scripts/storage_inventory.py`: its MISSING list (DB paths with no bucket object) and its per-category layout report surface any still-legacy `support_tickets` keys, and the deterministic legacy->new mapping is documented in the 2026-08-04 plan. Found 2026-08-05 (storage-migration review) |
| TD-073 | Flutter never asks for thumbnails, so the `_thumb` egress win is web-only | medium | mobile | `item_repository.dart:683` and `outfit_repository.dart:556` both resolve `image_url ?? thumbnail_url`, i.e. the full-size object, and `ItemModel` keeps a single `url` — so the wardrobe and outfit grids (the highest-volume image surfaces in the app) download full-size bytes per tile even with `THUMBNAIL_SERVING=true`. That is the exact per-fetch multiplier the thumbnail variant exists to remove (avg object ~0.94 MB vs a 512px tile). Fix: carry both URLs onto `ItemModel`/`OutfitModel`, render `thumbnail_url` in grids, and pass the full size as `AppNetworkImage.fallbackUrl` (the mechanism already exists and is used by `astrology_tab.dart`). Found 2026-08-05 (code-review pass) |
| TD-074 | Two image widgets cannot express a thumbnail fallback | low | cross | `AppImage` (Flutter) and `GeneratingSurface` (web) take a single URL/`string[]` with no error-fallback hook, so the thumbnail-404 fix could not be applied to them. Both call sites were switched to the full-size URL instead — correct, but it forgoes the thumbnail saving on the recommendations match grid (`find_matches_controller`, `recommendations_page`) and the outfit-generation preview strip (`OutfitDetailBody`). Fix: give `AppImage` a `fallbackUrl` mirroring `AppNetworkImage`, and let `GeneratingSurface` accept `(url, fallback)` pairs. Found 2026-08-05 (code-review pass) |
| TD-075 | Flutter version `3.44.6` is duplicated across five files | medium | mobile | There is no `.fvmrc`/`.tool-versions` pin, so the toolchain version is a literal string in `.github/workflows/build-apk.yml`, `build-ios.yml`, `flutter-ci.yml`, `shorebird-patch-ios.yml`, and `flutter/scripts/_shorebird_common.sh`. A bump must touch all five or CI and local builds silently diverge — and with Shorebird that divergence is worse than cosmetic, because a release's Flutter version can never be changed by a patch. Fix: adopt a single pin (FVM or a shared workflow env) and read it everywhere. Found 2026-08-06 (Shorebird integration) |
| TD-076 | `build-apk.yml` produces a debug-signed, unpatchable APK | low | infra | The Android CI workflow runs plain `flutter build apk --release` with no `key.properties` present, so `android/app/build.gradle.kts` falls back to the **debug** signing config, and the artifact contains no Shorebird engine. That is fine as fast PR feedback, which is its purpose, but the artifact must never reach Play — a store upload has to come from `flutter/scripts/shorebird_release_android.sh`. The workflow does not say so anywhere. Fix: rename the artifact to make it obviously non-shippable, or add a note in the job summary. Found 2026-08-06 (Shorebird integration) |
| TD-077 | No force-update / minimum-version gate exists | medium | mobile | Shorebird code push covers Dart-only fixes, but a native, plugin, or asset fix still requires a store release, and there is no mechanism to compel users onto it — no version check, no remote config, no minimum-supported-version response from the backend. Users on an old build stay broken indefinitely. Fix: have the backend return a minimum supported version and gate the splash on it. Found 2026-08-06 (Shorebird integration) |

| TD-078 | Hand-written `admin/src/shared/api/types.ts` partially superseded by generated `schema.d.ts` | low | admin | The M3 placeholder contract predates the OpenAPI codegen; `schemaTypes.ts` re-exports the generated models, but `types.ts` (admin roles, Paginated, auth shapes, blog types) still carries consumers. Migrate remaining imports to `schemaTypes.ts`/`schema.d.ts`, then delete. Found 2026-08-07 (admin panel wave 3) |
| TD-079 | Placeholder i18n namespace still used for page titles | low | admin | `routes.tsx` titles use `placeholder:*` keys (`placeholder:users.title`, …) while the content editor already has a real `content:` namespace. Move titles into per-feature namespaces as copy is finalized; the `placeholder` namespace exists as a stopgap only. Found 2026-08-07 (admin panel wave 3) |
| TD-080 | react-router 7 CSRF advisory (GHSA-qwww-vcr4-c8h2) | low | admin | Advisory covers RSC-mode action handling, patched only in 8.x. **Not exploitable in this app** — client-only library-mode SPA (no RSC, no server-rendered actions); all mutations go to the FastAPI backend with bearer auth. Revisit when a 7.x patch or an 8.x migration lands. Found 2026-08-07 (admin panel wave 3) |
| TD-081 | No granular field-level permissions (role-level only) | low | admin | Spec §12 out-of-scope: RBAC grants at permission granularity (`users.write`, `subscriptions.refund`, …), not per-field. Adding field-level control later means a backend model change + registry + tests. Found 2026-08-07 (admin panel wave 3) |
| TD-082 | Admin Playwright e2e not wired into CI + token-refresh verification pending | medium | admin | Spec §10 journeys landed 2026-08-08: 6 e2e specs / 8 critical journeys (login/logout, 403 page, user search/suspend, refund, storage cleanup, theme/density persistence) run via `npm run e2e` against a `vite preview` build with Playwright route interception, but are NOT wired into CI. The session store's 401 → silent refresh → retry → logout path is implemented but token-refresh is still not verified end-to-end. Land CI wiring with the hardening wave; until then the ledger marks admin stories unit-verified only. Found 2026-08-07 (admin panel wave 3) |

| TD-083 | ~~JSONB `contains(list)` filters 500: array literal vs JSON literal~~ | high | backend | **Fixed 2026-08-07** — `GET /items?occasion=informal` 500ed on every ~3s mobile poll: postgrest-py serializes `contains(list)` as a Postgres ARRAY literal (`cs.{informal}`) which a JSONB column cannot cast (`22P02 invalid input syntax for type jsonb`), and `is_db_connection_error`'s `invalid input` text marker (added 08-03 for h2 state strings) misclassified the deterministic query error as a dead pooled connection → 2 client rebuilds + 3 attempts → 500. RCA: `docs/exec-plans/active/2026-08-07-items-occasion-500-rca.md`. Fix: `jsonb_contains()` helper in `app/utils/db.py` (JSON-encodes list values) applied to `items.py` (`occasion_tags`, `colors`) and `outfits.py` (`tags`); `is_db_connection_error` returns `False` for `postgrest.exceptions.APIError` carrying SQLSTATE/PGRST codes so deterministic structured DB errors never trigger pool rebuilds, while a bare HTTP-status `code` (429/500/502/503/520/521/522/524 — a non-PostgREST-JSON gateway 5xx/429 body) stays retryable. Tests in `test_jsonb_contains.py` + `test_db_connection_retry.py`. Found 2026-08-07 |

| TD-084 | Refresh-token race causes 401 cascades on app resume | medium | cross | "Refresh token already used" (Supabase rotates refresh tokens; a second parallel refresh with the same token fails) → `/auth/refresh` 401 followed by a cascade of downstream 401s (`/items`, `/users/me`, `/subscription/usage`, `/referral/code` — observed 2026-08-07 11:13/13:49). Web and mobile each need a single-flight token refresh (share one in-flight refresh promise; serialize refreshes across tabs) plus a re-login path when rotation has already happened elsewhere. Backend-side it is benign (401s are correct); the fix is client-side. RCA: `docs/exec-plans/active/2026-08-07-prod-log-rca.md`. Found 2026-08-07 |
| TD-085 | Admin endpoints surface known PostgREST errors as 500 + full traceback spam | low | backend | `/admin/quotas` and `/admin/dashboards/*` raised raw `postgrest.exceptions.APIError` (PGRST200 no relationship, 42703 missing column) → catch-all handler logged the full traceback twice per request, which at 3 concurrent requests tripped Railway's 500 logs/sec limit (168 dropped, observed 2026-08-07 15:38). The underlying causes are fixed (quota embed rewrite; schema-gap markers now include PGRST204), but a residual gap remains: map known PostgREST schema errors in the admin routes to a structured 5xx with the operator hint instead of the generic INTERNAL_ERROR + traceback. Found 2026-08-07 |
| TD-086 | `ui-vendor` chunk stays on the public marketing critical path | medium | web | `ui-vendor` (~125 KB raw / ~35 KB gz: radix dialog + dropdown-menu + select) is statically reachable from every public pageview via Navbar's mobile `Sheet` (dialog), `ThemeToggle` (dropdown-menu), plus `LoginPromptModal`/`UpgradePromptDialog` (dialog). PSI on /blog flags ~25 KB of it unused. Removing it requires replacing the navbar `Sheet` + `ThemeToggle` with lightweight custom components (a11y: focus trap, escape, aria) and load-on-open for the two dialogs — all-or-nothing for the chunk, deferred from the 2026-08-07 pagespeed pass. Found 2026-08-07 |
| TD-087 | Blog post/category pages serve the empty app shell to users | medium | web | `/blog/:slug` and `/blog/category/*` cannot be prerendered at build time (slugs unknown); users get `app-shell.html` → first paint waits for JS, and crawlers receive head-tags-only from the `seo-html` edge function (no body content in the HTML). Option: prerender the top-N latest posts at build time (slugs resolvable from the public API, same bake pattern as the blog index) for real HTML + content in page source. Found 2026-08-07 (blog pagespeed RCA) |
| TD-088 | PostHog config.js served with 5-minute Cache-TTL | low | web | PSI "efficient cache lifetimes" flags `us-assets.i.posthog.com/…/config.js` (5m TTL, ~1 KiB) on every pageview. Fix would proxy PostHog assets through our origin with longer cache headers (edge function) — negligible byte savings, deferred. Found 2026-08-07 (blog pagespeed RCA) |
| TD-089 | Unidentified client sends out-of-range query params to `/items` → 422 | low | cross | `GET /api/v1/items` 422 at 91ms (observed 2026-08-07 17:52): FastAPI query-param validation after the ~90ms `get_active_user_id` lookup, so the client sent `page`/`page_size` outside 1–100 or a non-coercible `is_favorite`. Web sends ≤24 and Flutter sends `limit=20` + `page_size=20`, so neither shipped client matches — audit clients for out-of-range pagination before changing anything server-side (backend validation is correct). Found 2026-08-07 (late-window prod log RCA) |

| TD-090 | Session-cached presigned avatar URL breaks after 1h | low | mobile | `AuthController.user.avatarUrl` is materialized at login and cached for the session, so avatar tiles (dashboard header, profile edit) render an expired presigned URL after `OBJECT_STORAGE_PRESIGN_TTL`. Cosmetic only — try-on generation uses the server-side avatar, not the cached URL. Fix: refresh the user from `/users/me` when the profile/dashboard opens (same pattern as the item/outfit refresh-on-open), or re-mint the avatar via `/images/presigned`. Found 2026-08-08 (sibling-bug sweep, deferred Tier 3) |
| TD-091 | `createItemWithImage` leaves an orphan item when the image upload throws | low | mobile | `item_repository.dart::createItemWithImage` creates the item row first, then uploads; on upload failure the exception propagates and the row stays image-less (visible failure, not silent loss — the caller's catch means a retry creates a duplicate). Fix: delete the created row on upload failure, or upload-then-create. Found 2026-08-08 (sibling-bug sweep, deferred Tier 3) |
| TD-092 | `batchCreateItems` is dead code with silent image-loss baked in | low | mobile | `item_repository.dart::batchCreateItems` has no callers anywhere in `flutter/lib`; its shape would silently drop images (no per-image failure handling). Delete it (with its tests, if any) when someone next touches the repository. Found 2026-08-08 (sibling-bug sweep, deferred Tier 3) |
| TD-093 | Outfit edit page trips the ListTile-in-DecoratedBox debug assertion | low | mobile | `outfit_edit_page.dart` renders `ListTile`s inside `AppGlassCard` (a DecoratedBox with a background color), so debug builds report "ListTile background color or ink splashes may be invisible" on every render — harmless visually (release unaffected) but it blocks widget-testing the page (every pump reports the framework error). Fix: wrap those ListTiles in their own `Material`. Found 2026-08-08 (sibling-bug sweep — blocked the outfit-edit widget test; refresh-on-open is covered at the controller level) |

## Process

- Add rows when you consciously defer work.  
- Remove or strike through when fixed (leave a short “fixed YYYY-MM-DD” note or delete).  
- Monthly GC: see `docs/exec-plans/README.md`.
