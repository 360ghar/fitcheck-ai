# iOS 1.2 App Store release

Status: active  
Started: 2026-09-25  
Owner: agent

## Goal

Release the current Flutter app as `1.2.0+13`, upload the signed IPA to App Store Connect, and submit the version for App Review.

## Acceptance

- [x] Bump the version and write accurate release notes.
- [x] Fix the iOS workflow so GitHub can start a build job.
- [x] Pass Flutter checks and build an Apple Distribution-signed IPA.
- [x] Confirm App Store Connect accepted build 13 and attach it to version 1.2.0.
- [ ] Submit version 1.2.0 for App Review.
- [x] Record the release result, artifact, and remaining owner action.

## Context

- Source: `flutter/pubspec.yaml`, `flutter/CHANGELOG.md`, `flutter/metadata/release_notes.txt`.
- Build: `.github/workflows/build-ios.yml`, `flutter/scripts/build_ios_release.sh`.
- Store setup: `flutter/metadata/app_store_checklist.md`, `docs/store/app-store-listing.md`.
- The GitHub repository has no configured Apple signing or App Store Connect upload secrets. Its [iOS workflow](https://github.com/360ghar/fitcheck-ai/actions/runs/36119906141) passed an unsigned validation run.
- Xcode is signed in to the Apple Developer account. The Apple Distribution-signed IPA is at `flutter/build/ios/ipa/FitCheck AI.ipa`. The local export plist uses `destination=export`; a temporary upload plist sent the archive to App Store Connect on 2026-09-25.
- [Build 13](https://appstoreconnect.apple.com/teams/58083fb1-cef0-435c-8922-b51452d9b3b1/apps/6794689012/testflight/ios/e9895aff-3214-421f-819f-c21d524294e7) is Ready to Submit and attached to version 1.2.0, which is Ready for Review in a draft submission.
- Shorebird sign-in did not complete. This release uses standard Flutter and cannot receive Shorebird code patches. A later patchable release needs a new version and build through `flutter/scripts/build_ios_release.sh`.
- App Review sign-in credentials and contact phone/email are blank. Supply working values before submitting the review draft.

## Verification

The CI dispatch below validates the unsigned iOS build only. An upload-enabled
dispatch needs the Apple signing and App Store Connect secrets listed in
`.github/workflows/build-ios.yml`. The local Xcode archive, signed IPA export,
and App Store Connect upload succeeded for this build.

```bash
(cd flutter && flutter analyze --no-fatal-infos --no-fatal-warnings && flutter test --exclude-tags golden)
python3 scripts/check_ios_deployment_target.py
gh workflow run build-ios.yml --ref codex/review-app-for-issues-and-bugs -f upload_to_testflight=false
```
