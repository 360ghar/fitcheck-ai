# Plan: PR 22 review and CI fixes

Status: active  
Started: 2026-09-25  
Owner: agent

## Goal

Validate all comments on PR 22, fix the remaining issues, and pass the checks on the pushed commit. Include and verify the existing compact item cutout changes as requested.

## Acceptance criteria

- [x] Validate the 22 open threads against the code and record each result.
- [x] Cover async state, onboarding, and test environment bugs with regression tests.
- [x] Fix docs, backend, and Android CI failures without disabling checks.
- [ ] Run local checks, commit all changes, and push the PR branch.
- [ ] Confirm the GitHub checks and deployment previews pass for the final commit.

## Context

- PR: <https://github.com/360ghar/fitcheck-ai/pull/22>
- Existing changes: `2026-09-25-compact-item-cutouts.md`.
- 61 review threads were already resolved before this pass.

## Progress log

| Date | Note |
|------|------|
| 2026-09-25 | Read all review threads and CI results. Confirmed the checkout matches the PR head. Found 22 open threads and three failed CI jobs. |

## Decision log

| Date | Decision | Why |
|------|----------|-----|
| 2026-09-25 | Preserve and verify the existing workspace edits. | The user requested all changes be committed and pushed. |

## Verification

```bash
./scripts/check_all.sh --include-frontend-build
(cd admin && npm run lint && npm run typecheck && npm test && npm run build && npm run check:schema && npm run check:bundle && npm run e2e)
(cd widgets && npm run build)
npx --yes markdownlint-cli2 --config .markdownlint.json "docs/**/*.md" "AGENTS.md" "ARCHITECTURE.md" "README.md"
gh pr checks 22 --repo 360ghar/fitcheck-ai
```

## Review results

All 22 open comments were valid. The timer fix also needs explicit timer cancellation: auto-dispose alone cannot cancel a pending `Future.delayed`. The 61 earlier resolved threads retain their existing fixes and scope decisions (web has no translation framework; completed work belongs under `completed/`).

| Comment | Fix and evidence |
|---------|------------------|
| Future setup dates | Both Flutter predicates and the web predicate reject negative ages; boundary tests pass. |
| Tiny deckle steps | Limit the path to 2,048 generated tear points and require each increment to advance. Invalid and tiny steps return finite paths. |
| Shelf edit regression | Widget test checks that the edited piece leaves Tops and joins Bottoms. |
| Verification working directory | Brand plan commands use subshells. |
| Android dark system splash | Add `values-night-v31` with the shared splash background. |
| Web setup docs | State account age, missing gender, local completion flag, and return URL behavior. |
| Closet request docs | State initial shared fetch plus on-demand pagination. |
| Gender keyboard input | Use native radio inputs; arrow keys and single tab-stop behavior pass. |
| Late preferences | Ignore hydration after the user edits choices; delayed-response test passes. |
| Setup garment grain | Keep scene grain and its stock background; inspect and update the two affected goldens. |
| Setup return URL | Preserve path, query, and fragment; resume on Skip setup or Later. Test safe and rejected destinations. |
| Stale photoshoot failure | Check the retry session before error reporting; current and reset-flow tests pass. |
| Interrupted pagination | Clear the pending flag before invalidating; failed refresh retains items and allows paging again. |
| AbortSignal constructor | Use `signal.constructor`; static methods, `instanceof`, and native Request tests pass. |
| Debt header | Update date and range through TD-118. |
| Calendar formatting | Split AppBar fields and run Dart formatting. |
| Stale reward diagnostics | Log before the state guard; stale failure test keeps the newer UI. |
| Sidebar wordmark motion | Animate width, opacity, and gap; keep the accessible link name and reduced-motion support. |
| Mobile completion scope | Document the per-account, per-device flag. |
| Empty gender coverage | Add the missing case and seven-day boundary coverage. |
| Scene grain contract | Document transparent background plus foreground ancestor grain. |
| Greeting timer disposal | Auto-dispose both providers and cancel the explicit timer; widget unmount test passes. |

## CI failure fixes

- Markdown: correct the bare PR URL in the PR 21 plan.
- Backend: the existing image optimization passes the unchanged speed gate. Also fix the generation caller to keep a successful crop of an already-transparent image; a failing regression demonstrated the discarded bytes.
- Android: use Gradle 8.14, Android Gradle Plugin 8.11.1, and Kotlin 2.2.20, matching the minimum versions in the installed Flutter 3.47.5 dependency checker.

## Local verification results

- Backend full suite: 4,317 passed, 4 skipped, 94.65% coverage before the additional cropped-result regression. Ruff and API spec drift checks pass.
- Web: 395 tests pass; lint and production build pass. The build could not fetch optional blog data in the sandbox; its generated sitemap changes were discarded.
- Admin: lint, types, tests, build, schema drift, and bundle budget pass. Installed the missing Playwright Chromium binary for local browser verification.
- Flutter: analysis reports no issues; 380 non-golden tests pass. Existing compact-grid goldens pass. The two setup scene goldens changed as expected.
- Docs, architecture, migrations, theme tokens, iOS target, widget build, and 59 image worker tests pass.
